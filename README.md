
*Unofficial notes*
### Express.js CRUD Routes
```
CRUD        METHOD    PATH
CREATE      GET       /urls/new
CREATE      POST      /urls
READ (ALL)  GET       /urls
READ (ONE)  GET       /urls/:id
UPDATE      GET       /urls/:id/edit
UPDATE      POST      /urls/:id
DELETE      POST      /urls/:id/delete          

```